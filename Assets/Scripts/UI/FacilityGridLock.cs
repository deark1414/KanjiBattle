using UnityEngine;

// Facility layout is now owned by FacilityListUI.
// This component is intentionally kept empty to avoid stale script references
// while preventing the old fixed-width late update from overriding the layout.
public sealed class FacilityGridLock : MonoBehaviour
{
}
